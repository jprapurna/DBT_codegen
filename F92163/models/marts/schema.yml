Based on the provided SQL files, none of them qualify as "marts" SQL files. All the files reference intermediate models (`int_abc__bal_row_counts`, `int_stg__tfplcy_tran_result`, `int_pre_fdr__fire_plcy_trans_rslt`) and are therefore excluded from schema.yml generation as per your guidelines.

If you have any other SQL files that qualify as marts, please provide them, and I will generate the corresponding `schema.yml`.