version: 2

sources:
  - name: W_CLAIM_CD_SCD3_IU
    description: Workflow generated by PC2Cloud : wf_W_CNTC_TELEPHONY_AGENT_D_SCD3_IU
    database: W_CLAIM_CD_SCD3_IU
    schema: null

    tables:
      - name: SQ_CDH_GW_BUR
        description: Source Query for CDH_GW_BUR
        identifier: DUMMY_SQ_CDH_GW_BUR

        columns:
          - name: POLICY_STATE
            description: State of the policy
            tests:
              - not_null
              - unique
          - name: BUR
            description: Business Unit Reference
            tests:
              - not_null
              - unique
          - name: SOURCE_NAME
            description: Source name for the data
            tests:
              - not_null
              - unique

      - name: LKP_W_CLAIM_CD_BUR_SCD3
        description: Lookup for W_CLAIM_CD_BUR_SCD3
        identifier: DUMMY_LKP_W_CLAIM_CD_BUR_SCD3

        columns:
          - name: LKP_ROW_WID
            description: Lookup Row Wide Identifier
            tests:
              - not_null
              - unique
          - name: LKP_INTEGRATION_ID
            description: Lookup Integration Identifier
            tests:
              - not_null
              - unique
          - name: LKP_NEW_BUR
            description: Lookup New Business Unit Reference
            tests:
              - not_null
              - unique

      - name: LKPK_CDM_BATCH_CTRLID
        description: Lookup for CDM Batch Control ID
        identifier: DUMMY_mplt_CDM_BATCH_ID_lkp_CDM_BATCH_CTRLID

      - name: LKPK_MAX_ROW_WID
        description: Lookup for Maximum Row Wide Identifier
        identifier: DUMMY_mplt_CDM_ROW_WID_lkp_MAX_ROW_WID

      - name: CDH_GW_BUR
        description: CDH Gateway Business Unit Reference
        identifier: CDH_GW_BUR

      - name: Customer
        description: Customer information
        identifier: Customer

      - name: Orders
        description: Order details
        identifier: Orders

      - name: Product
        description: Product information
        identifier: Product

      - name: Category
        description: Category details
        identifier: Category