version: 2

sources:
  - name: W_CLAIM_CD_SCD3_IU
    description: Workflow generated by PC2Cloud : wf_W_CNTC_TELEPHONY_AGENT_D_SCD3_IU
    database: W_CLAIM_CD_SCD3_IU
    schema: null
    tables:
      - name: SQ_CDH_GW_BUR
        description: Source table for CDH Gateway BUR data
        identifier: SQ_CDH_GW_BUR
        columns:
          - name: POLICY_STATE
            description: State of the policy
            tests:
              - not_null
          - name: BUR
            description: BUR data
            tests:
              - not_null
          - name: SOURCE_NAME
            description: Source name for the data
            tests:
              - not_null
              - unique
      - name: LKP_W_CLAIM_CD_BUR_SCD3
        description: Lookup table for W_CLAIM_CD_BUR_SCD3
        identifier: LKP_W_CLAIM_CD_BUR_SCD3
        columns:
          - name: lkp_ROW_WID
            description: Lookup ROW_WID
            tests:
              - not_null
              - unique
          - name: lkp_INTEGRATION_ID
            description: Lookup Integration ID
            tests:
              - not_null
          - name: lkp_NEW_BUR
            description: Lookup new BUR data
            tests:
              - not_null
      - name: lkp_CDM_BATCH_CTRLID
        description: Lookup table for CDM Batch Control ID
        identifier: lkp_CDM_BATCH_CTRLID
        columns:
          - name: BATCH_ID
            description: Batch ID
            tests:
              - not_null
              - unique
          - name: SOURCE_NAME
            description: Source name for the batch
            tests:
              - not_null
      - name: lkp_MAX_ROW_WID
        description: Lookup table for maximum ROW_WID
        identifier: lkp_MAX_ROW_WID
        columns:
          - name: ROW_WID
            description: Maximum ROW_WID
            tests:
              - not_null
              - unique
          - name: TABLE_NAME
            description: Target table name
            tests:
              - not_null
      - name: CDH_GW_BUR
        description: CDH Gateway BUR data table
        identifier: CDH_GW_BUR
        columns:
          - name: POLICY_STATE
            description: State of the policy
            tests:
              - not_null
          - name: BUR
            description: BUR data
            tests:
              - not_null
          - name: SOURCE_NAME
            description: Source name for the data
            tests:
              - not_null
              - unique