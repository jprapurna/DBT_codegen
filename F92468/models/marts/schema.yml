version: 2

models:
  - name: fct_claims  # Required
    description: A fact table containing claims data, sourced from the intermediate model `int_claims__exp_row_wid`.
    meta:
      owner: data_team  # Optional metadata
      domain: insurance
    config:
      materialized: table
    tags: [insurance, claims]
    columns: []
``` 

### Explanation:
- The SQL file `fct_claims.sql` uses `SELECT *` to pull all columns from the intermediate model `int_claims__exp_row_wid`. Since no specific columns are explicitly defined in the SELECT clause, no column-level metadata or tests can be inferred.
- The model name is derived from the file name (`fct_claims.sql`).
- The description provides a high-level summary of the model's purpose.
- No columns are listed because the SQL does not explicitly define them.