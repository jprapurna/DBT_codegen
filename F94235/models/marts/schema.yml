Based on the provided SQL files, the models are intermediate or staging models (`int_claim_cd__upd_bur` and `int_claim_cd__rtr_clm_insert_upd`) and do not qualify as marts. Therefore, no `schema.yml` file will be generated for these models. 

If you have any mart-level SQL files, please provide them for schema generation.